const user = require('./user');
const role = require('./role');
const server = require('./Server');
const Category = require('./category');
const Product = require('./product')

module.exports = {
    user, role, server, Category,Product
}